apiVersion: v1
kind: Service
metadata:
  name: pocket-tarot-server-dev
  labels:
    app.kubernetes.io/name: pocket-tarot-server
    app.kubernetes.io/component: api
    app.kubernetes.io/instance: dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pocket-tarot-server
    app.kubernetes.io/component: api
    app.kubernetes.io/instance: dev
  ports:
    - name: http
      port: 80
      targetPort: http
